"""Capabilities: which parts of a vendor's surface and conduct are switched
on. INVARIANT: off is answered, not hidden -- see :mod:`.registry`. Its
companion in :mod:`.gates`: silence is a failure."""
