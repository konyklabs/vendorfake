"""Configuration: the profile document, the environment layer, and the
result -- precedence is built-in defaults, caller defaults, profile
document, environment, decided once into :class:`ResolvedConfig`."""
