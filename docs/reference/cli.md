<!-- GENERATED by `tools/gen_reference.py` from `vendorfake.cli._build_parser()` (`--help`, verbatim). Do not hand-edit; re-run `uv run python tools/gen_reference.py`. -->

# CLI

Every subcommand's `--help`.

## `vendorfake`

```text
usage: vendorfake [-h] [--json] [--version] COMMAND ...

Run or describe a high-fidelity fake of a third-party vendor API.

positional arguments:
  COMMAND
    serve        Serve a unit over HTTP.
    info         Print what a unit would be, as JSON, without serving it.
    openapi      Print the OpenAPI 3.1 document for a unit's route table.
    vendors      List the vendors that would resolve here.
    profiles     List the profiles a vendor ships.
    routes       List a vendor's route table.
    faults       List the built-in fault catalogue.
    explain      Explain one route, fault, profile, error kind, or
                 Vendorfake-* header.
    conformance  Run the conformance contracts against a unit.

options:
  -h, --help     show this help message and exit
  --json         Machine output: one JSON document on stdout, nothing else on
                 stdout.
  --version      Print the distribution version and exit.

Unofficial. Not affiliated with, endorsed by, or connected to any vendor whose
public API a module here imitates.
```

## `vendorfake serve`

```text
usage: vendorfake serve [-h] [--vendor VENDOR] [--profile PROFILE]
                        [--host HOST] [--port PORT] [--log-level LOG_LEVEL]

options:
  -h, --help            show this help message and exit
  --vendor VENDOR       Vendor to serve (see `vendorfake vendors`). Defaults
                        to $VENDORFAKE_VENDOR; with exactly one vendor
                        installed that one is used, otherwise the command
                        refuses and lists them.
  --profile PROFILE     Profile name or path. Defaults to $VENDORFAKE_PROFILE,
                        then to the vendor's default profile.
  --host HOST           Interface to bind. Defaults to $VENDORFAKE_HOST, then
                        loopback.
  --port PORT           Port to bind; 0 picks a free one and prints it.
                        Defaults to $VENDORFAKE_PORT, then 8080.
  --log-level LOG_LEVEL
                        uvicorn log level. Defaults to $VENDORFAKE_LOG_LEVEL,
                        then the profile's.
```

## `vendorfake info`

```text
usage: vendorfake info [-h] [--json] [--vendor VENDOR] [--profile PROFILE]

options:
  -h, --help         show this help message and exit
  --json             Machine output: one JSON document on stdout, nothing else
                     on stdout.
  --vendor VENDOR    Vendor to serve (see `vendorfake vendors`). Defaults to
                     $VENDORFAKE_VENDOR; with exactly one vendor installed
                     that one is used, otherwise the command refuses and lists
                     them.
  --profile PROFILE  Profile name or path. Defaults to $VENDORFAKE_PROFILE,
                     then to the vendor's default profile.
```

## `vendorfake openapi`

```text
usage: vendorfake openapi [-h] [--json] [--vendor VENDOR] [--profile PROFILE]
                          [--no-internal]

options:
  -h, --help         show this help message and exit
  --json             Machine output: one JSON document on stdout, nothing else
                     on stdout.
  --vendor VENDOR    Vendor to serve (see `vendorfake vendors`). Defaults to
                     $VENDORFAKE_VENDOR; with exactly one vendor installed
                     that one is used, otherwise the command refuses and lists
                     them.
  --profile PROFILE  Profile name or path. Defaults to $VENDORFAKE_PROFILE,
                     then to the vendor's default profile.
  --no-internal      Omit the /__unit/* control plane, describing only the
                     vendor surface.
```

## `vendorfake vendors`

```text
usage: vendorfake vendors [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json      Machine output: one JSON document on stdout, nothing else on
              stdout.
```

## `vendorfake profiles`

```text
usage: vendorfake profiles [-h] [--json] [--vendor VENDOR]

options:
  -h, --help       show this help message and exit
  --json           Machine output: one JSON document on stdout, nothing else
                   on stdout.
  --vendor VENDOR  Vendor to describe. Defaults to $VENDORFAKE_VENDOR; with
                   exactly one vendor installed that one is used, otherwise
                   the command refuses and lists them.
```

## `vendorfake routes`

```text
usage: vendorfake routes [-h] [--json] [--vendor VENDOR] [--profile PROFILE]
                         [--internal]

options:
  -h, --help         show this help message and exit
  --json             Machine output: one JSON document on stdout, nothing else
                     on stdout.
  --vendor VENDOR    Vendor to describe. Defaults to $VENDORFAKE_VENDOR; with
                     exactly one vendor installed that one is used, otherwise
                     the command refuses and lists them.
  --profile PROFILE  Profile to build the table against. The table itself does
                     not vary by profile; see the docstring of
                     vendorfake.registry.routes. Defaults to
                     $VENDORFAKE_PROFILE, then 'full'.
  --internal         Include the /__unit/* control plane. Omitted by default:
                     this is the vendor surface.
```

## `vendorfake faults`

```text
usage: vendorfake faults [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json      Machine output: one JSON document on stdout, nothing else on
              stdout.
```

## `vendorfake explain`

```text
usage: vendorfake explain [-h] [--json] KIND ...

positional arguments:
  KIND
    route     One route, by operation_id.
    fault     One fault, by name.
    profile   One profile, by name.
    error     One core error kind, by name.
    header    One Vendorfake-* header, by name.

options:
  -h, --help  show this help message and exit
  --json      Machine output: one JSON document on stdout, nothing else on
              stdout.
```

## `vendorfake conformance`

```text
usage: vendorfake conformance [-h] ...

positional arguments:
  rest        Arguments forwarded to the conformance runner.

options:
  -h, --help  show this help message and exit
```
